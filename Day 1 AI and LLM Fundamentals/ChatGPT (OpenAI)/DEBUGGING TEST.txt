DEBUGGING TEST:

Prompt:
Имам тази грешка в Laravel: [paste error message]. Как да я поправя?

Response:

Разбирам 👍
В Laravel грешката

```
Illuminate\Validation\ValidationException
```

не е „бъг“ в кода на Laravel, а означава, че **валидаторът е отхвърлил входните данни**. Тоест някое поле, което минава през `Validator` или `$request->validate([...])`, не отговаря на зададените правила.

---

## Как да я поправиш

1. **Провери правилата за валидация**
   В контролера или FormRequest ще има нещо такова:

   ```php
   $validated = $request->validate([
       'egn' => ['required', 'digits:10'],
       'email' => ['required', 'email'],
       // ...
   ]);
   ```

   Ако входът не отговаря (напр. `egn` ≠ 10 цифри), Laravel хвърля `ValidationException`.

2. **Виж съобщенията за грешка**
   По време на dev можеш да dump-неш грешките:

   ```php
   try {
       $validated = $request->validate([...]);
   } catch (\Illuminate\Validation\ValidationException $e) {
       dd($e->errors());
   }
   ```

   Това ще покаже точно кое поле е невалидно и защо.

3. **Поправи входните данни или правилото**

   * Ако правилата са твърде строги → коригирай ги.
   * Ако входните данни са грешни → подай правилните данни (например 10-цифрен ЕГН, валиден email и т.н.).

4. **Във фронтенд (Blade / Vue / React)**
   Важно е да показваш грешките на потребителя:

   ```blade
   @error('egn')
       <div class="text-red-500">{{ $message }}</div>
   @enderror
   ```

---

## Чести примери

* Ако използваш твоите функции `validateEGN`/`parseEGN` като custom rule, трябва да ги вържеш правилно:

  ```php
  Validator::extend('egn', function ($attribute, $value, $parameters, $validator) {
      return validateEGN($value);
  });
  ```

  и после:

  ```php
  $request->validate([
      'egn' => ['required', 'egn'],
  ]);
  ```

* Ако липсва задължително поле (`required`) → ще се хвърли `ValidationException`.

---

👉 С две думи: грешката е сигнал, че **валидаторът е хванал невалидни данни**.
За да я оправиш:

* провери `$request->validate([...])` правилата,
* виж `dd($e->errors())`,
* и коригирай входа или правилата.

---

Искаш ли да ти покажа пример как точно да интегрираш твоята PHP функция `validateEGN` като **Laravel validation rule**, за да валидираш ЕГН директно в `FormRequest`?
