EXPLANATION TEST:

Prompt:
Обясни dependency injection в Laravel като че ли съм junior developer.


Response:

Окей 🙂 Нека ти обясня **Dependency Injection (DI) в Laravel** по възможно най-простия начин.

---

## 1. Какво значи „Dependency“?

„Dependency“ е зависимост – тоест, когато един клас използва друг клас, за да работи.
Например:

```php
class UserController {
    protected $service;

    public function __construct(UserService $service) {
        $this->service = $service;
    }

    public function index() {
        return $this->service->getAllUsers();
    }
}
```

Тук `UserController` **зависи** от `UserService`, за да върне потребители.

---

## 2. Какво значи „Injection“?

„Injection“ е начинът, по който тази зависимост се „вкарва“ (инжектира) в класа.
Вместо да правиш това вътре в контролера:

```php
$this->service = new UserService();
```

Laravel сам ще създаде `UserService` и ще ти го даде чрез конструктора.

---

## 3. Защо е полезно?

* **По-малко „твърдо кодиране“** – ако вътре правиш `new UserService()`, после трябва да променяш навсякъде кода, ако искаш друга имплементация.
* **Лесно тестване** – можеш да подадеш фалшив (mock) `UserService` при тестове.
* **Ясен код** – ясно се вижда какво точно изисква класа, като погледнеш конструктора.

---

## 4. Как Laravel прави магията?

Laravel има **Service Container** – това е „кутия“, в която държи всички класове и зависимости.
Когато кажеш:

```php
public function __construct(UserService $service)
```

Laravel:

1. Вижда, че ти трябва `UserService`.
2. Проверява дали знае как да го създаде.
3. Ако може – го създава и ти го подава.

---

## 5. Пример с Route и контролер

```php
Route::get('/users', [UserController::class, 'index']);
```

Laravel ще:

* Създаде `UserController`.
* Ще види, че контролерът иска `UserService`.
* Ще създаде `UserService`.
* Ще го „инжектира“ в контролера.

---

👉 Накратко: **Dependency Injection е начин Laravel автоматично да ти дава нужните обекти, вместо ти да ги създаваш ръчно.** Това прави кода по-чист, по-гъвкав и по-лесен за тестване.

---

Искаш ли да ти направя малък пример с **interface + две различни имплементации**, за да видиш колко удобно става със Service Container?
